- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.bash_aliases: linux/bash_aliases
    ~/.bash_logout: linux/bash_logout
    ~/.bashrc: linux/bashrc
    ~/.profile: linux/profile
