##########
# Common #
##########

# some more ls aliases
alias lla="ls -lAF"
alias ll='ls -lF'
alias la='ls -AF'

# Force tmux to use 256 colors
alias tmux='tmux -2'

alias tml='tmux ls'
alias tma='tmux a -t'
alias tmn='tmux new -s'
