- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.bash_aliases: bash_aliases
    ~/.bash_logout: bash_logout
    ~/.bashrc: bashrc
    ~/.gitconfig: gitconfig
    ~/.profile: profile
    ~/.vimrc: vimrc
